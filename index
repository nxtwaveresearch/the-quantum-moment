<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>The Quantum Moment — Live with Matt McCall · NXT Wave Research</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,900;1,700&family=Cormorant+Garamond:ital,wght@1,700&family=Rubik:wght@300;400;500;600&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg-deep:     #060E10;
      --bg-surface:  #0C1A1E;
      --bg-card:     #132028;
      --green:       #0F7E6E;
      --green-glow:  rgba(15,126,110,0.18);
      --green-light: #13A08C;
      --teal-bright: #1CB8A0;
      --gold:        #B8904A;
      --gold-light:  #D4AA6A;
      --silver:      #8AABB8;
      --white:       #FFFFFF;
      --muted:       rgba(255,255,255,0.55);
      --border:      rgba(255,255,255,0.08);
    }

    html { scroll-behavior: smooth; }

    body {
      background: radial-gradient(ellipse at 50% 30%, #0A2028 0%, #060E10 60%);
      color: var(--white);
      font-family: 'Rubik', sans-serif;
      font-weight: 400;
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* ─── QUANTUM PROCESSOR BACKGROUND ─── */
    .qbg {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      pointer-events: none;
      z-index: 0;
      overflow: hidden;
    }
    .qbg svg {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -46%);
      width: max(140vw, 900px);
      height: auto;
      opacity: 0.12;
    }

    /* ─── NAV ─── */
    nav {
      position: relative;
      z-index: 10;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px 40px;
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      font-family: 'Rubik', sans-serif;
      font-weight: 600;
      font-size: 13px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--muted);
    }
    .nav-logo span { color: var(--green); }

    .nav-badge {
      background: rgba(15,126,110,0.15);
      border: 1px solid rgba(15,126,110,0.4);
      color: var(--green-light);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 5px 12px;
      border-radius: 100px;
    }

    /* ─── HERO ─── */
    .hero {
      position: relative;
      z-index: 1;
      max-width: 860px;
      margin: 0 auto;
      padding: 72px 40px 20px;
      text-align: center;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--green-light);
      margin-bottom: 28px;
    }
    .hero-eyebrow::before,
    .hero-eyebrow::after {
      content: '';
      display: block;
      width: 28px;
      height: 1px;
      background: var(--green);
      opacity: 0.6;
    }

    .hero-title {
      font-family: 'Playfair Display', serif;
      font-weight: 900;
      font-size: clamp(44px, 6.5vw, 72px);
      line-height: 1.08;
      letter-spacing: -0.01em;
      margin-bottom: 20px;
    }
    .hero-title .quantum-word {
      font-family: 'Cormorant Garamond', serif;
      font-style: italic;
      font-weight: 700;
      color: #5EFAD8;
      font-size: 1.08em;
    }

    .hero-subhead {
      font-family: 'Rubik', sans-serif;
      font-weight: 400;
      font-size: clamp(18px, 2.5vw, 24px);
      color: rgba(255,255,255,0.72);
      line-height: 1.4;
      margin-bottom: 28px;
      letter-spacing: 0;
    }

    /* ─── QUANTUM CIRCUIT ILLUSTRATION ─── */
    .circuit-wrap {
      max-width: 680px;
      margin: 0 auto 32px;
      opacity: 0.9;
    }
    .circuit-wrap svg { width: 100%; height: auto; display: block; }

    .hero-meta {
      display: flex;
      align-items: center;
      justify-content: center;
      flex-wrap: wrap;
      gap: 6px 20px;
      font-size: 15px;
      color: var(--muted);
      margin-bottom: 20px;
    }
    .hero-meta span { display: flex; align-items: center; gap: 6px; }
    .hero-meta .dot {
      width: 3px; height: 3px;
      background: var(--border);
      border-radius: 50%;
    }

    /* ─── BULLETS ─── */
    .bullets {
      display: flex;
      flex-direction: column;
      gap: 12px;
      max-width: 640px;
      margin: 0 auto 56px;
      text-align: left;
    }
    .bullet {
      display: flex;
      align-items: flex-start;
      gap: 14px;
      background: rgba(15,126,110,0.07);
      border: 1px solid rgba(15,126,110,0.18);
      border-radius: 10px;
      padding: 14px 18px;
      font-size: 17px;
      line-height: 1.6;
    }
    .bullet-icon {
      flex-shrink: 0;
      width: 22px;
      height: 22px;
      background: var(--green);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-top: 1px;
    }
    .bullet-icon svg { width: 11px; height: 11px; fill: white; }


    /* ─── BULLETS SECTION (standalone below signup) ─── */
    .bullets-section {
      position: relative;
      z-index: 1;
      max-width: 640px;
      margin: 0 auto;
      padding: 0 40px 64px;
    }
    .bullets-section .bullets {
      margin-bottom: 0;
    }

    /* ─── SIGNUP CARD ─── */
    .signup-section {
      position: relative;
      z-index: 1;
      max-width: 520px;
      margin: 0 auto;
      padding: 0 40px 48px;
    }

    .signup-card {
      background: var(--bg-surface);
      border: 1px solid rgba(15,126,110,0.3);
      border-radius: 16px;
      padding: 36px 36px 32px;
      box-shadow: 0 0 60px rgba(15,126,110,0.1), 0 24px 48px rgba(0,0,0,0.4);
    }

    .signup-card h3 {
      font-family: 'Playfair Display', serif;
      font-size: 26px;
      font-weight: 700;
      margin-bottom: 8px;
    }
    .signup-card .signup-sub {
      font-size: 15px;
      color: var(--muted);
      margin-bottom: 24px;
    }

    .field-group { display: flex; flex-direction: column; gap: 12px; margin-bottom: 16px; }

    .field-label {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 5px;
    }

    input[type="text"],
    input[type="email"] {
      width: 100%;
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 13px 16px;
      font-family: 'Rubik', sans-serif;
      font-size: 17px;
      color: var(--white);
      outline: none;
      transition: border-color 0.2s;
    }
    input[type="text"]:focus,
    input[type="email"]:focus {
      border-color: var(--green);
    }
    input::placeholder { color: rgba(255,255,255,0.25); }

    .btn-reserve {
      width: 100%;
      background: var(--green);
      color: white;
      border: none;
      border-radius: 8px;
      padding: 15px 24px;
      font-family: 'Rubik', sans-serif;
      font-size: 17px;
      font-weight: 600;
      letter-spacing: 0.02em;
      cursor: pointer;
      transition: background 0.2s, transform 0.1s;
      margin-top: 4px;
    }
    .btn-reserve:hover { background: var(--green-light); }
    .btn-reserve:active { transform: scale(0.99); }
    .btn-reserve:disabled { opacity: 0.6; cursor: not-allowed; }

    .privacy-note {
      text-align: center;
      font-size: 11px;
      color: var(--muted);
      margin-top: 14px;
    }

    /* ─── SUCCESS STATE ─── */
    .success-state {
      display: none;
      text-align: center;
      padding: 20px 0 4px;
    }
    .success-state.visible { display: block; }
    .success-check {
      width: 52px; height: 52px;
      background: rgba(15,126,110,0.15);
      border: 2px solid var(--green);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
    }
    .success-check svg { width: 22px; height: 22px; stroke: var(--green-light); fill: none; stroke-width: 2.5; stroke-linecap: round; stroke-linejoin: round; }
    .success-state h4 {
      font-family: 'Playfair Display', serif;
      font-size: 20px;
      margin-bottom: 8px;
    }
    .success-state p { font-size: 14px; color: var(--muted); }

    /* ─── DIVIDER ─── */
    .section-divider {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto 64px;
      padding: 0 40px;
    }
    .divider-line {
      border: none;
      border-top: 1px solid var(--border);
      margin-bottom: 64px;
    }

    /* ─── BODY COPY ─── */
    .body-section {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto;
      padding: 0 40px 72px;
    }

    .alert-banner {
      background: rgba(15,126,110,0.1);
      border-left: 3px solid var(--green);
      border-radius: 0 8px 8px 0;
      padding: 16px 20px;
      font-size: 15px;
      font-weight: 500;
      margin-bottom: 36px;
      color: rgba(255,255,255,0.9);
    }

    .body-section p {
      font-size: 18px;
      line-height: 1.8;
      color: rgba(255,255,255,0.8);
      margin-bottom: 20px;
    }

    .body-section p strong {
      color: var(--white);
      font-weight: 600;
    }

    .closing-line {
      font-size: 15px;
      font-style: italic;
      color: var(--muted);
      margin-top: 32px;
      padding-top: 24px;
      border-top: 1px solid var(--border);
    }

    /* ─── WHAT MATT WILL COVER ─── */
    .coverage-section {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto 72px;
      padding: 0 40px;
    }

    .section-label {
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--green);
      margin-bottom: 24px;
    }

    .coverage-list { display: flex; flex-direction: column; gap: 16px; }

    .coverage-item {
      display: flex;
      gap: 20px;
      align-items: flex-start;
    }
    .coverage-num {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      font-weight: 700;
      color: rgba(15,126,110,0.4);
      line-height: 1;
      min-width: 32px;
    }
    .coverage-text { font-size: 17px; color: rgba(255,255,255,0.8); padding-top: 4px; }

    /* ─── BIO ─── */
    .bio-section {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto 80px;
      padding: 0 40px;
    }
    .bio-card {
      background: var(--bg-surface);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 28px;
      display: flex;
      gap: 22px;
      align-items: flex-start;
    }
    .bio-avatar {
      flex-shrink: 0;
      width: 52px; height: 52px;
      background: var(--green);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Playfair Display', serif;
      font-size: 18px;
      font-weight: 700;
      color: white;
    }
    .bio-name { font-weight: 600; font-size: 16px; margin-bottom: 4px; }
    .bio-title { font-size: 12px; color: var(--green-light); letter-spacing: 0.06em; text-transform: uppercase; margin-bottom: 10px; }
    .bio-copy { font-size: 14px; color: var(--muted); line-height: 1.65; }

    /* ─── BOTTOM CTA ─── */
    .bottom-cta {
      position: relative;
      z-index: 1;
      max-width: 520px;
      margin: 0 auto 80px;
      padding: 0 40px;
      text-align: center;
    }
    .bottom-cta p {
      font-size: 14px;
      font-style: italic;
      color: var(--muted);
      margin-top: 14px;
    }
    .btn-reserve-outline {
      display: inline-block;
      width: 100%;
      background: transparent;
      border: 2px solid var(--green);
      color: var(--green-light);
      border-radius: 8px;
      padding: 15px 24px;
      font-family: 'Rubik', sans-serif;
      font-size: 17px;
      font-weight: 600;
      letter-spacing: 0.02em;
      cursor: pointer;
      transition: background 0.2s, color 0.2s;
      text-decoration: none;
    }
    .btn-reserve-outline:hover { background: var(--green); color: white; }

    /* ─── FOOTER ─── */
    footer {
      position: relative;
      z-index: 1;
      border-top: 1px solid var(--border);
      text-align: center;
      padding: 24px 40px;
      font-size: 12px;
      color: rgba(255,255,255,0.25);
    }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 600px) {
      nav { padding: 16px 20px; }
      .hero { padding: 48px 20px 40px; }
      .bullets { padding: 0 0px; }
      .signup-section, .body-section, .coverage-section, .bio-section, .bottom-cta { padding-left: 20px; padding-right: 20px; }
      .signup-card { padding: 28px 22px 24px; }
      .bio-card { flex-direction: column; gap: 14px; }
    }
  </style>
</head>
<body>

  <!-- QUANTUM PROCESSOR BACKGROUND -->
  <div class="qbg" aria-hidden="true">
    <svg viewBox="0 0 1000 1000" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <radialGradient id="bgGlow" cx="50%" cy="50%" r="50%">
          <stop offset="0%" stop-color="#1A6060" stop-opacity="0.35"/>
          <stop offset="100%" stop-color="#060E10" stop-opacity="0"/>
        </radialGradient>
      </defs>

      <!-- ambient glow at center -->
      <ellipse cx="500" cy="500" rx="320" ry="320" fill="url(#bgGlow)"/>

      <!-- ── OUTERMOST RING ── -->
      <circle cx="500" cy="500" r="430" fill="none" stroke="#8AABB8" stroke-width="1.2" stroke-opacity="0.25"/>
      <!-- bolt holes on outer ring -->
      <circle cx="500" cy="70"  r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="500" cy="930" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="70"  cy="500" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="930" cy="500" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="197" cy="197" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="803" cy="197" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="197" cy="803" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <circle cx="803" cy="803" r="7" fill="none" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>

      <!-- ── SECOND RING ── -->
      <circle cx="500" cy="500" r="340" fill="none" stroke="#B8904A" stroke-width="1.4" stroke-opacity="0.3"/>

      <!-- ── THIRD RING (gold platform) ── -->
      <circle cx="500" cy="500" r="250" fill="none" stroke="#D4AA6A" stroke-width="2" stroke-opacity="0.35"/>

      <!-- ── INNER RING ── -->
      <circle cx="500" cy="500" r="160" fill="none" stroke="#B8904A" stroke-width="1.6" stroke-opacity="0.4"/>

      <!-- ── CORE RING ── -->
      <circle cx="500" cy="500" r="80"  fill="none" stroke="#1CB8A0" stroke-width="1.8" stroke-opacity="0.5"/>
      <circle cx="500" cy="500" r="40"  fill="none" stroke="#1CB8A0" stroke-width="1"   stroke-opacity="0.35"/>
      <circle cx="500" cy="500" r="12"  fill="#1CB8A0" fill-opacity="0.2"/>

      <!-- ── CURVED SPOKES (arching up like the image) ──
           Each spoke: starts at inner ring ~r=80, curves outward and arches
           upward before landing on outer ring ~r=430.
           Using quadratic bezier with control point lifted above the chord. -->

      <!-- Spoke angles: 0°,20°,40°,60°,80°,100°,120°,140°,160°,180°,200°,220°,240°,260°,280°,300°,320°,340° -->
      <!-- Gold spokes (primary, thicker) -->
      <path d="M500,420 Q440,180 500,70"   fill="none" stroke="#D4AA6A" stroke-width="1.4" stroke-opacity="0.55"/>
      <path d="M500,580 Q560,820 500,930"   fill="none" stroke="#D4AA6A" stroke-width="1.4" stroke-opacity="0.55"/>
      <path d="M420,500 Q180,440 70,500"    fill="none" stroke="#D4AA6A" stroke-width="1.4" stroke-opacity="0.55"/>
      <path d="M580,500 Q820,560 930,500"   fill="none" stroke="#D4AA6A" stroke-width="1.4" stroke-opacity="0.55"/>

      <!-- Diagonal gold spokes -->
      <path d="M443,443 Q270,250 197,197"  fill="none" stroke="#B8904A" stroke-width="1.2" stroke-opacity="0.45"/>
      <path d="M557,443 Q730,250 803,197"  fill="none" stroke="#B8904A" stroke-width="1.2" stroke-opacity="0.45"/>
      <path d="M443,557 Q270,750 197,803"  fill="none" stroke="#B8904A" stroke-width="1.2" stroke-opacity="0.45"/>
      <path d="M557,557 Q730,750 803,803"  fill="none" stroke="#B8904A" stroke-width="1.2" stroke-opacity="0.45"/>

      <!-- Silver spokes (secondary, thinner, between gold) -->
      <path d="M472,422 Q390,160 430,72"   fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M528,422 Q610,160 570,72"   fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M472,578 Q390,840 430,928"  fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M528,578 Q610,840 570,928"  fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M422,472 Q160,390 72,430"   fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M422,528 Q160,610 72,570"   fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M578,472 Q840,390 928,430"  fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>
      <path d="M578,528 Q840,610 928,570"  fill="none" stroke="#8AABB8" stroke-width="0.8" stroke-opacity="0.35"/>

      <!-- Teal accent spokes (diagonal secondary) -->
      <path d="M455,428 Q310,195 240,145"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M545,428 Q690,195 760,145"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M455,572 Q310,805 240,855"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M545,572 Q690,805 760,855"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M428,455 Q195,310 145,240"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M428,545 Q195,690 145,760"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M572,455 Q805,310 855,240"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>
      <path d="M572,545 Q805,690 855,760"  fill="none" stroke="#1CB8A0" stroke-width="0.8" stroke-opacity="0.3"/>

      <!-- ── QUBIT PINS on platform ring (r=250) ── -->
      <!-- Small gold cylinders/dots where spokes meet the inner platform -->
      <circle cx="500" cy="250" r="5" fill="#D4AA6A" fill-opacity="0.7"/>
      <circle cx="500" cy="750" r="5" fill="#D4AA6A" fill-opacity="0.7"/>
      <circle cx="250" cy="500" r="5" fill="#D4AA6A" fill-opacity="0.7"/>
      <circle cx="750" cy="500" r="5" fill="#D4AA6A" fill-opacity="0.7"/>
      <circle cx="323" cy="323" r="4" fill="#D4AA6A" fill-opacity="0.6"/>
      <circle cx="677" cy="323" r="4" fill="#D4AA6A" fill-opacity="0.6"/>
      <circle cx="323" cy="677" r="4" fill="#D4AA6A" fill-opacity="0.6"/>
      <circle cx="677" cy="677" r="4" fill="#D4AA6A" fill-opacity="0.6"/>

      <!-- Extra qubit dots between -->
      <circle cx="404" cy="263" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="596" cy="263" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="404" cy="737" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="596" cy="737" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="263" cy="404" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="263" cy="596" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="737" cy="404" r="3" fill="#B8904A" fill-opacity="0.55"/>
      <circle cx="737" cy="596" r="3" fill="#B8904A" fill-opacity="0.55"/>

      <!-- Teal qubit dots on inner ring -->
      <circle cx="500" cy="340" r="3.5" fill="#1CB8A0" fill-opacity="0.6"/>
      <circle cx="500" cy="660" r="3.5" fill="#1CB8A0" fill-opacity="0.6"/>
      <circle cx="340" cy="500" r="3.5" fill="#1CB8A0" fill-opacity="0.6"/>
      <circle cx="660" cy="500" r="3.5" fill="#1CB8A0" fill-opacity="0.6"/>
      <circle cx="387" cy="387" r="3"   fill="#1CB8A0" fill-opacity="0.5"/>
      <circle cx="613" cy="387" r="3"   fill="#1CB8A0" fill-opacity="0.5"/>
      <circle cx="387" cy="613" r="3"   fill="#1CB8A0" fill-opacity="0.5"/>
      <circle cx="613" cy="613" r="3"   fill="#1CB8A0" fill-opacity="0.5"/>


      <!-- ── CIRCUIT DIAGRAM LINES (woven into background, top-left corner) ── -->
      <g opacity="0.5">
        <!-- Horizontal qubit lines -->
        <line x1="20" y1="60"  x2="200" y2="60"  stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <line x1="20" y1="90"  x2="200" y2="90"  stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <line x1="20" y1="120" x2="200" y2="120" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <!-- Gate boxes -->
        <rect x="50" y="53"  width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <rect x="50" y="83"  width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <rect x="50" y="113" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <!-- CNOT -->
        <line x1="110" y1="60" x2="110" y2="90" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.45"/>
        <circle cx="110" cy="60" r="3.5" fill="#1CB8A0" fill-opacity="0.45"/>
        <circle cx="110" cy="90" r="6"   fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.45"/>
        <!-- Measurement boxes -->
        <rect x="155" y="53"  width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <rect x="155" y="83"  width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <rect x="155" y="113" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
      </g>

      <!-- ── CIRCUIT LINES (bottom-right corner) ── -->
      <g opacity="0.5">
        <line x1="800" y1="880" x2="980" y2="880" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <line x1="800" y1="910" x2="980" y2="910" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <line x1="800" y1="940" x2="980" y2="940" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <rect x="820" y="873" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <rect x="820" y="903" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <rect x="820" y="933" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.5"/>
        <line x1="880" y1="880" x2="880" y2="910" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.45"/>
        <circle cx="880" cy="880" r="3.5" fill="#1CB8A0" fill-opacity="0.45"/>
        <circle cx="880" cy="910" r="6"   fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.45"/>
        <rect x="930" y="873" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <rect x="930" y="903" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
        <rect x="930" y="933" width="18" height="14" rx="2" fill="none" stroke="#1CB8A0" stroke-width="0.7" stroke-opacity="0.4"/>
      </g>

      <!-- ── HORIZONTAL CROSSBAR LINES (like the flat connectors in image) ── -->
      <line x1="70"  y1="480" x2="70"  y2="520" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <line x1="930" y1="480" x2="930" y2="520" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <line x1="480" y1="70"  x2="520" y2="70"  stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
      <line x1="480" y1="930" x2="520" y2="930" stroke="#8AABB8" stroke-width="1" stroke-opacity="0.3"/>
    </svg>
  </div>

  <!-- NAV -->
  <nav>
    <div class="nav-logo">NXT <span>Wave</span> Research</div>
    <div class="nav-badge">Free Event · June 4</div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-eyebrow">Live Briefing · Matt McCall</div>

    <h1 class="hero-title">
      The <span class="quantum-word">Quantum</span> Moment
    </h1>

    <p class="hero-subhead">What the Government's $2B Bet Means for Your Portfolio</p>

    <div class="hero-meta">
      <span>📅 Thursday, June 4</span>
      <span class="dot"></span>
      <span>5:00 PM ET</span>
      <span class="dot"></span>
      <span>Free to Attend</span>
    </div>

  </section>

  <!-- SIGN-UP CARD -->
  <div class="signup-section" id="signup">
    <div class="signup-card">
      <h3>Reserve Your Seat</h3>
      <p class="signup-sub">Thursday, June 4 · 5:00 PM ET · Live with Matt McCall</p>

      <div id="form-area">
        <div class="field-group">
          <div>
            <div class="field-label">Email Address</div>
            <input type="email" id="femail" placeholder="you@example.com" />
          </div>
        </div>
        <button class="btn-reserve" id="reserve-btn" onclick="handleSignup()">
          Reserve Your Seat →
        </button>
        <p class="privacy-note">Free to attend. No credit card required.</p>
      </div>

      <div class="success-state" id="success-area">
        <div class="success-check">
          <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <h4>You're registered!</h4>
        <p>Check your inbox — we'll send the event link before Thursday's briefing.</p>
      </div>
    </div>
  </div>

  <!-- BULLETS -->
  <div class="bullets-section">
    <div class="bullets">
      <div class="bullet">
        <div class="bullet-icon">
          <svg viewBox="0 0 12 12"><polyline points="2,6 5,9 10,3"/></svg>
        </div>
        The 3 quantum stocks Matt is watching right now — including one the government list left off
      </div>
      <div class="bullet">
        <div class="bullet-icon">
          <svg viewBox="0 0 12 12"><polyline points="2,6 5,9 10,3"/></svg>
        </div>
        The IPO pricing this week that could be the entry point of the year
      </div>
      <div class="bullet">
        <div class="bullet-icon">
          <svg viewBox="0 0 12 12"><polyline points="2,6 5,9 10,3"/></svg>
        </div>
        Why the biggest quantum opportunity isn't the one everyone's talking about
      </div>
    </div>
  </div>

  <!-- BODY COPY -->
  <div class="body-section">
    <p>Last week, the Trump administration quietly committed <strong>$2 billion to quantum computing companies</strong> — and took ownership stakes in return. The stocks moved <strong>25–33% in a day</strong>, then the headlines moved on. But Matt McCall was paying attention. He's seen this playbook before — semiconductors, nuclear energy, rare earth miners — and every time, the investors who understood what the government was <em>actually signaling</em> made serious money before it became obvious. Quantum is next, and this Thursday Matt is going live to walk through the specific stocks, the layer of the market nobody is talking about, and what could be the biggest quantum IPO in history.</p>
  </div>



  <!-- BIO -->
  <div class="bio-section">
    <div class="bio-card">
      <div class="bio-avatar">MM</div>
      <div>
        <div class="bio-name">Matt McCall</div>
        <div class="bio-title">Founder, NXT Wave Research</div>
        <div class="bio-copy">Founder of NXT Wave Research and one of the most recognized names in early-stage investing. Matt has spent decades identifying disruptive companies before Wall Street catches on.</div>
      </div>
    </div>
  </div>

  <!-- BOTTOM CTA -->
  <div class="bottom-cta">
    <a class="btn-reserve-outline" href="#signup" onclick="document.getElementById('femail').focus()">
      Reserve Your Seat →
    </a>
    <p>This is one of those weeks where being early makes all the difference.</p>
  </div>

  <!-- FOOTER -->
  <footer>
    © 2026 NXT Wave Research · All Rights Reserved
  </footer>

  <script>
    // ── CONFIGURATION ──────────────────────────────────────────────
    // Replace with your actual Google Apps Script Web App URL
    const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbysrgYJfr3d72BU9CIAX_HFuwo_1VAWX4A2H7leI1IyBwC_GA6W836LrNMi_EU2-1U8/exec';
    const EVENT_NAME = 'The Quantum Moment — June 4, 2026';
    // ───────────────────────────────────────────────────────────────

    async function handleSignup() {
      const femail = document.getElementById('femail').value.trim();
      const btn    = document.getElementById('reserve-btn');

      if (!femail) {
        alert('Please enter your email address to reserve your seat.');
        return;
      }
      if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(femail)) {
        alert('Please enter a valid email address.');
        return;
      }

      btn.disabled = true;
      btn.textContent = 'Reserving…';

      try {
        await fetch(SCRIPT_URL, {
          method: 'POST',
          mode: 'no-cors',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            email:     femail,
            event:     EVENT_NAME,
            timestamp: new Date().toISOString()
          })
        });
      } catch (err) {
        console.warn('Fetch error (may be CORS):', err);
      }

      document.getElementById('form-area').style.display = 'none';
      const success = document.getElementById('success-area');
      success.classList.add('visible');
    }

    document.addEventListener('keydown', function(e) {
      if (e.key === 'Enter' && document.activeElement.id === 'femail') {
        handleSignup();
      }
    });
  </script>
</body>
</html>
